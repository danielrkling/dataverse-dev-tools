{
  "name": "dataverse-webresource-ide",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host",
    "build": "vite build",
    "test": "playwright test",
    "test:ui": "playwright test --ui"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.1",
    "vite": "^5.0.0"
  },
  "dependencies": {
    "esbuild-wasm": "^0.28.1"
  }
}
